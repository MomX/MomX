# should be splitted

- is_attached()
- attach()



core

check installed
compare versions cran
compare versions github

MomX_refresh_from_CRAN
MomX_refresh (last passing)
MomX_refresh_last

attach1
attach(core)

detach(1)
detach(core)

MomX as a tibble :-)
~package, ~version, ~commits, ~CRAN

MomX_breathe
MomX_refresh # if minor (*.me.*) release ahed
MomX_relax
MomX_feel
refresh_packages

library() triggers


   core_df <- tibble(core, here, cran, github)
   check installed if not intall
                      yes load

MomX_breathe() -> get CRAN, get gh version,
MomX_refresh() -> get last passing github
MomX_released() -> get_last_released






